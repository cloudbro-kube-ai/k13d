name: create-namespace
description: Create a new namespace
difficulty: easy
prompt: |
  Create a new namespace named 'production' with the following labels:
  - environment=production
  - team=platform
setup: setup.sh
verifier: verify.sh
cleanup: cleanup.sh
timeout: 120
