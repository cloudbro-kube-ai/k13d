apiVersion: v1
kind: Service
metadata:
  name: {{ include "k13d.fullname" . }}
  labels:
    {{- include "k13d.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "k13d.selectorLabels" . | nindent 4 }}
